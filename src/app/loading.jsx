import LoadingSpinner from '@/components/LoadingSpiner';
import React from 'react';

const loading = () => {
    return (
        <div>
            <LoadingSpinner/>
        </div>
    );
};

export default loading;